version: '3.8'

volumes:
  development_postgres_data: {}
  development_postgres_data_backups: {}
  staticfiles:
  uploads:

services:
  django:
    build:
      context: .
      dockerfile: ./compose/development/django/Dockerfile
    image: rard_development_django
    expose:
      - 5000
    container_name: django
    depends_on:
      - postgres
      - elasticsearch
    links:
      - postgres
      - elasticsearch
    volumes:
      - type: volume
        source: uploads
        target: /uploads
      - type: volume
        source: staticfiles
        target: /static
    env_file:
      - ./.envs/.development/.django
      - ./.envs/.development/.postgres
    command: /start

  postgres:
    build:
      context: .
      dockerfile: ./compose/development/postgres/Dockerfile
    image: rard_development_postgres
    container_name: postgres
    volumes:
      - type: volume
        source: development_postgres_data
        target: /var/lib/postgresql/data
      - type: volume
        source: development_postgres_data_backups
        target: /backups
    env_file:
      - ./.envs/.development/.postgres

  elasticsearch:
    container_name: elasticsearch
    image: elasticsearch:7.4.0
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
    ports:
      - 9200:9200

  pgadmin:
    build:
      context: .
      dockerfile: ./compose/development/pgadmin/Dockerfile
    image: rard_development_pgadmin
    expose:
      - 80
    env_file:
      - ./.envs/.development/.pgadmin
    depends_on:
      - postgres

  nginx:
    build:
      context: .
      dockerfile: ./compose/development/nginx/Dockerfile
    image: rard_development_nginx
    ports:
      - "0.0.0.0:80:80"
      - "0.0.0.0:443:443"
      - "0.0.0.0:2345:2345"
    volumes:
      - type: bind
        source: ./compose/development/nginx/conf.d
        target: /etc/nginx/conf.d
      - type: bind
        source: ./compose/development/nginx/certs
        target: /etc/nginx/certs
      - type: bind
        source: ./compose/development/nginx/snippets
        target: /etc/nginx/snippets
      - type: volume
        source: staticfiles
        target: /static
    depends_on:
      - django
      - pgadmin
      - elasticsearch 

